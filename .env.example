# Focusly 环境配置模板
# 复制为 .env 后按需修改（修改后需重启 npm run dev）

# 基础地址 /api（留空时默认同源 /api，可配合 vite 代理或同源 Mock；
# 无服务时自动降级 LocalStorage 兜底）
VITE_API_BASE_URL=
# Apifox Mock 演示模式（含 /api 前缀，示例）
# VITE_API_BASE_URL=https://m1.apifoxmock.com/m1/1234567-0-default/api

VITE_API_TIMEOUT=3000
